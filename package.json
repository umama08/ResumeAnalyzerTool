{
  "name": "resume-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack resume screening MVP for fintech recruitment workflows",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "samples": "npm run samples --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
